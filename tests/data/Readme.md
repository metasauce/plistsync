This directory contains data for testing the plistsync package. 

Folders:
- `audio` : Test audio files in a variety of formats.
    Audio files are untagged i.e. they have no metadata and contain 2s of silence. We opted to include some test files here instead of generating them on the fly to avoid the need for additional dependencies.